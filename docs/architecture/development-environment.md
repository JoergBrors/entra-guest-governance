# Development Environment

Stand: 2026-08-30

Lokale Entwicklung nutzt `LOCAL_MOCK` als Default.

Werkzeuge laut Repository:

- .NET 10 SDK
- Node.js/npm
- Vite
- React
- Fluent UI
- Cosmos DB Emulator optional fuer lokale Persistenz
- Docker + Compose optional als containerisierte Alternative (`docker-compose.yml`, kompletter `LOCAL_MOCK`-Stack ohne lokales SDK/Node.js)

Siehe auch `README.md` und `scripts/requirements.ps1`.

